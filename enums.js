//These properties are not meant to be configurable
var numberEnum = {
  DIGITS_12: 12,
  DIGITS_4: 4,
  ROMANS: "numerals"
}

var clockTickEnum = {
  continuous: "continuous",
  tick: "tick"
}
